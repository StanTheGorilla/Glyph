{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and HUD overlay",
  "windows": ["main", "hud"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
